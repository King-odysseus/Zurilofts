# Codex assignment: host/admin implementation and integration

Implement phases 3-5 of `design2.md` in the ZuriLofts repository, starting from Claude's committed phases 0-2 result. This is an implementation request. Read `AGENTS.md`, `Instruct.md`, `design2.md`, `docs/design2-implementation-status.md`, current source and the local preview PNGs before editing.

First verify Claude's handoff and commits exist. If the first assignment is still running or absent, report that prerequisite and do not begin competing edits to shared files. Once it is available, preserve the completed guest work and use its shared components.

Keep the approved former colours: bronze `#C49A6C` primary actions (hover `#B8895C`, white text), navy `#0B0B45` strong secondary actions, neutral outlined secondary actions and blue `#2563EB` links/selected states/focus. Translate blue primary buttons in the boards to bronze; do not recolour blue indicators. Preserve the existing logo. Do not edit `DESIGN.md` or `design2.md` to weaken the target.

Visual references are the PNG boards beside `C:/Users/Mega-Mind/Documents/ZuriLofts Design Preview/START-HERE.html`. Open boards 03 and 09-14 and relevant guest panels. Follow `design2.md` for explicit layout/behaviour requirements and intentional palette/logo/data differences. Missing reference files must be reported, not treated as visually verified.

Own the Codex route rows in section 6: H1/H2 host Today, onboarding, listing management/editor, weekly/monthly calendars, earnings/payouts; A1 admin overview, listings/calendar/forms, bookings/finance/users, reviews/disputes/feedback and content/support. Finish role-specific shells/navigation. Preserve matching components and replace incomplete structural approximations, including decorative form steps or booking-only queues presented as combined operational queues.

Check both host and admin rendering whenever editing shared AdminProperties, AdminPropertyForm, AdminCalendar or AdminEarnings. Preserve ownership/permissions, draft/publication distinctions, reserved-date rules, privacy, real quote/payment/payout semantics and mandatory validation. Add only narrowly necessary data integration with appropriate tests; never simulate missing capabilities or loosen authorisation to match a picture. No new dependencies without approval.

Then run phase 5 across the whole app, including Claude's guest routes. Fix demonstrated cross-page integration and fidelity defects in shared navigation, styles, router or guest components when needed; preserve the design contracts and recapture affected screens. Do not replace the guest implementation with a competing design.

Follow section 8 using a working local/test backend and permitted accounts: desktop/tablet/mobile screenshots, route/state coverage, keyboard/mobile checks, guest-to-booking flow, host operational flows and admin scope/privacy checks. Run build/lint and relevant server tests for changed behaviour. Do not exercise real production charges, payouts, deletions or moderation to produce evidence. Report inaccessible states honestly and continue independent work.

Extend `docs/design2-implementation-status.md` into a consolidated report covering every registered route (54 at baseline), including both host/admin variants of reused pages. Provide exact screenshot links, environment/commit/viewport context, tested behaviours and command results. Mark unresolved requirements partial/blocked with cause and next action; do not declare the full redesign complete with unverified protected screens or unimplemented interactions.

Check active work and git status, preserve unrelated changes, commit/push logical batches per project rules, and keep screenshots outside the repository by default. Do not deploy. Finish with implementation commits, the consolidated evidence report, screenshot locations and remaining gaps. Your self-check prepares the result for the user's later independent audit; it does not replace it.

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Server environment
server/.env
server/node_modules
server/dist

# Admin-uploaded property images (runtime data, not source)
server/uploads

# Agent folders and instructions — must never live in the project repo
Instruct.md
.claude/
.kilo/
.qwen/
.continue/
.cursor/

# Developer manual (local only, separate React app)
manual/

# Offline Android APK shell (local only — imports parent source)
offline-app/

# Claude Code local settings (hooks, permissions per machine)
.claude/
.kimi/
.kimi-code/
.agents/

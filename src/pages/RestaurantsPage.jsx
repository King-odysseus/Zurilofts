import { lazy, Suspense, useMemo, useState } from 'react';
import { HomeHeader } from './HomePage.jsx';
import Footer from '../components/Footer.jsx';
import Dropdown from '../components/Dropdown.jsx';
import Spinner from '../components/Spinner.jsx';
import { AREAS, EAT_CATEGORIES, PLACES_TO_EAT } from '../data/nearby.js';
import { googleMapsDirectionsUrl } from '../utils/googleMaps.js';

const NearbyMap = lazy(() => import('../components/NearbyMap.jsx'));

function RestaurantCard({ item }) {
  const directionsUrl = googleMapsDirectionsUrl({ ...item, label: item.mapsQuery || `${item.name}, Nairobi, Kenya`, preferLabel: true });
  return <article className="group min-w-0 overflow-hidden rounded-2xl bg-white shadow-[0_8px_24px_rgba(11,31,66,0.07)] ring-1 ring-[#E3E8EF] transition-shadow hover:shadow-[0_12px_30px_rgba(11,31,66,0.13)]"><div className="relative aspect-[3/2] overflow-hidden"><img src={item.image} alt={item.name} className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105" loading="lazy" /><span className="absolute left-3 top-3 rounded-full bg-white/90 px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[.08em] text-[#0B1F42] backdrop-blur-sm">{AREAS[item.area] || item.area}</span></div><div className="p-4"><p className="text-[11px] font-semibold uppercase tracking-[.1em] text-[#B8895C]">{EAT_CATEGORIES[item.category] || item.category}</p><h2 className="mt-1.5 text-base font-semibold text-[#0B1F42]">{item.name}</h2><p className="mt-2 line-clamp-3 text-xs leading-5 text-[#5B6B82]">{item.desc}</p><a href={directionsUrl} target="_blank" rel="noopener noreferrer" className="mt-4 inline-flex min-h-10 items-center rounded-full bg-[#C49A6C] px-4 text-xs font-semibold text-white hover:bg-[#B8895C]">Get directions <span className="ml-1.5" aria-hidden="true">↗</span></a></div></article>;
}

function RestaurantsPage() {
  const [query, setQuery] = useState(''); const [area, setArea] = useState('all'); const [category, setCategory] = useState('all'); const [viewMode, setViewMode] = useState('list'); const [filtersOpen, setFiltersOpen] = useState(false);
  const filtered = useMemo(() => { const q = query.trim().toLowerCase(); return PLACES_TO_EAT.filter((item) => (area === 'all' || item.area === area) && (category === 'all' || item.category === category) && (!q || `${item.name} ${item.desc} ${item.area} ${item.category}`.toLowerCase().includes(q))); }, [area, category, query]);
  const areaOptions = Object.entries(AREAS).map(([value, label]) => ({ value, label })); const categoryOptions = Object.entries(EAT_CATEGORIES).map(([value, label]) => ({ value, label })); const hasFilters = area !== 'all' || category !== 'all' || query.trim(); const clearFilters = () => { setArea('all'); setCategory('all'); setQuery(''); };
  return <div className="min-h-screen overflow-x-hidden bg-[#F8FAFC] text-[#0B1F42]"><HomeHeader propertiesPage searchLabel="Search restaurants" searchPath="/restaurants" /><main><section className="mx-auto max-w-[1200px] px-4 pb-5 pt-7 md:px-8 md:pb-6 md:pt-8"><p className="text-xs text-[#5B6B82]">Home <span className="mx-1">›</span> Restaurants</p><h1 className="mt-2 text-2xl font-semibold tracking-tight md:text-[32px]">Places to eat in Nairobi</h1><p className="mt-2 max-w-2xl text-sm text-[#5B6B82] md:text-base">From Kenyan favourites to rooftop dining, find your next great meal.</p></section><section className="mx-auto max-w-[1200px] px-4 pb-6 md:px-8"><div className="flex min-h-14 w-full items-center rounded-full border border-[#E3E8EF] bg-white px-4 shadow-[0_8px_24px_rgba(11,31,66,0.06)] focus-within:border-[#C49A6C] focus-within:ring-2 focus-within:ring-[#C49A6C]/15"><svg className="mr-3 h-5 w-5 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><circle cx="11" cy="11" r="7" /><path d="m20 20-4-4" /></svg><label htmlFor="restaurant-search" className="sr-only">Search restaurants</label><input id="restaurant-search" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search restaurants, neighbourhoods or cuisines" className="min-w-0 flex-1 bg-transparent py-3 text-sm outline-none placeholder:text-[#94A3B8]" />{query && <button type="button" onClick={() => setQuery('')} className="px-2 text-lg text-[#64748B]" aria-label="Clear restaurant search">×</button>}</div></section><section className="mx-auto max-w-[1200px] px-4 pb-6 md:px-8"><div className="hidden items-center justify-between gap-4 md:flex"><div className="flex flex-wrap gap-2"><Dropdown value={area} onChange={setArea} options={areaOptions} triggerClassName="rounded-full border border-[#E3E8EF] bg-white px-4 py-2 text-xs font-medium text-[#33415C]" menuClassName="left-0" ariaLabel="Filter restaurants by area" /><Dropdown value={category} onChange={setCategory} options={categoryOptions} triggerClassName="rounded-full border border-[#E3E8EF] bg-white px-4 py-2 text-xs font-medium text-[#33415C]" menuClassName="left-0" ariaLabel="Filter restaurants by cuisine" /><button type="button" onClick={() => setViewMode(viewMode === 'map' ? 'list' : 'map')} className="rounded-full border border-[#E3E8EF] bg-white px-4 py-2 text-xs font-medium">{viewMode === 'map' ? 'Show list' : 'Show map'}</button>{hasFilters && <button type="button" onClick={clearFilters} className="rounded-full px-3 py-2 text-xs font-semibold text-[#B8895C]">Clear all</button>}</div><p className="text-sm text-[#5B6B82]">{filtered.length} restaurants</p></div><div className="md:hidden"><div className="grid grid-cols-2 gap-3"><button type="button" onClick={() => setFiltersOpen(!filtersOpen)} className="min-h-11 rounded-full border border-[#E3E8EF] bg-white text-xs font-semibold">Filters{hasFilters ? ' ·' : ''}</button><button type="button" onClick={() => setViewMode(viewMode === 'map' ? 'list' : 'map')} className="min-h-11 rounded-full border border-[#E3E8EF] bg-white text-xs font-semibold">{viewMode === 'map' ? 'Show list' : 'Show map'}</button></div>{filtersOpen && <div className="mt-3 flex flex-wrap gap-2 rounded-2xl border border-[#E3E8EF] bg-white p-3"><Dropdown value={area} onChange={setArea} options={areaOptions} triggerClassName="rounded-full border border-[#E3E8EF] px-3 py-2 text-xs" menuClassName="left-0" ariaLabel="Filter restaurants by area" /><Dropdown value={category} onChange={setCategory} options={categoryOptions} triggerClassName="rounded-full border border-[#E3E8EF] px-3 py-2 text-xs" menuClassName="left-0" ariaLabel="Filter restaurants by cuisine" /><button type="button" onClick={clearFilters} className="rounded-full px-3 py-2 text-xs font-semibold text-[#B8895C]">Clear all</button></div>}</div></section><section className="mx-auto max-w-[1200px] px-4 pb-16 md:px-8">{viewMode === 'map' && filtered.length ? <Suspense fallback={<div className="flex h-[420px] items-center justify-center rounded-2xl bg-white"><Spinner /></div>}><NearbyMap items={filtered} title="Places to eat in Nairobi" /></Suspense> : filtered.length ? <div className="grid grid-cols-1 gap-x-6 gap-y-8 sm:grid-cols-2 lg:grid-cols-4">{filtered.map((item) => <RestaurantCard key={item.name} item={item} />)}</div> : <div className="mx-auto max-w-md rounded-2xl border border-[#E3E8EF] bg-white p-8 text-center"><h2 className="text-base font-semibold">No restaurants match your search.</h2><button type="button" onClick={clearFilters} className="mt-5 min-h-11 rounded-full bg-[#0B1F42] px-6 text-sm font-semibold text-white">Clear filters</button></div>}</section></main><Footer /></div>;
}

export default RestaurantsPage;

{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && cd server && npm install && npx prisma generate --schema=prisma/schema.postgresql.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npx prisma migrate deploy --schema=prisma/schema.postgresql.prisma && node dist/index.js",
    "healthcheckPath": "/api/health"
  }
}

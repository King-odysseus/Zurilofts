{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && cp prisma/schema.postgresql.prisma prisma/schema.prisma && npx prisma generate && npm run build && cd .. && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && cp prisma/schema.postgresql.prisma prisma/schema.prisma && npx prisma db push && node dist/index.js",
    "healthcheckPath": "/api/health"
  }
}

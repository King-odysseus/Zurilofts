{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && npx prisma generate --schema=prisma/schema.postgresql.prisma && npm run build && cd .. && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npx prisma db push --schema=prisma/schema.postgresql.prisma && node dist/index.js",
    "healthcheckPath": "/api/health"
  }
}
